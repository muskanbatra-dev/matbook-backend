generator client {
  provider = "prisma-client-js"
  output   = "../generated"
}

datasource db {
  provider = "postgresql"
}

model Submission {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  data      Json
}
